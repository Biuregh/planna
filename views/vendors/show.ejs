<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vendor: <%= vendor.name %>
    </title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/layout.css" />
</head>

<body>
    <%- include("../partials/_logo") %>
    <div class="layout">
        <%- include("../partials/_navbar") %>
        <main class="content">
            <section class="vendor-show">
                <h1 class="heading">
                    <%= vendor.name %>
                </h1>
                <ul class="vendor-details">
                    <li><strong>Email:</strong>
                        <%= vendor.email %>
                    </li>
                    <li><strong>Phone:</strong>
                        <%= vendor.phone || "N/A" %>
                    </li>
                    <li><strong>Website:</strong>
                        <%= vendor.website || "N/A" %>
                    </li>
                    <li><strong>Address:</strong>
                        <%= vendor.address || "N/A" %>
                    </li>
                    <li><strong>City:</strong>
                        <%= vendor.city || "N/A" %>
                    </li>
                    <li><strong>State:</strong>
                        <%= vendor.state || "N/A" %>
                    </li>
                    <li><strong>ZIP:</strong>
                        <%= vendor.zip || "N/A" %>
                    </li>
                    <li><strong>Service Type:</strong>
                        <%= vendor.serviceType %>
                    </li>
                    <li><strong>Description:</strong>
                        <%= vendor.description || "N/A" %>
                    </li>
                    <li><strong>Claimed:</strong>
                        <%= vendor.claimed ? "Yes" : "No" %>
                    </li>
                </ul>
                <a class="btn btn-edit" href="/vendors/<%= vendor._id %>/edit">Edit Vendor</a>
            </section>
        </main>
    </div>
</body>

</html>