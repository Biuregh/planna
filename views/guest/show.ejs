<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= guest.firstName %>
        <%= guest.lastName %>
    </title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/layout.css" />
</head>

<body>
    <%- include("../partials/_logo") %>
    <div class="layout">
        <%- include("../partials/_navbar") %>
        <main class="content">
            <section class="guest-show">
                <h1 class="page-title" id="guest-show-title">Guest Details</h1>
                <div class="guest-details">
                    <p class="guest-name"><strong>Name:</strong>
                        <%= guest.firstName %>
                            <%= guest.lastName %>
                    </p>
                    <p class="guest-email"><strong>Email:</strong>
                        <%= guest.email %>
                    </p>
                    <p class="guest-phone"><strong>Phone:</strong>
                        <%= guest.phone %>
                    </p>
                    <p class="guest-group"><strong>Group:</strong>
                        <%= guest.group %>
                    </p>
                    <p class="guest-note"><strong>Note:</strong>
                        <%= guest.note %>
                    </p>
                    <a href="/guest/<%= guest._id %>/edit" class="btn edit-btn">Edit</a>
                    <form action="/guest/<%= guest._id %>?_method=DELETE" method="POST" class="delete-form">
                        <button type="submit" class="btn btn-delete">Delete</button>
                    </form>
                </div>
            </section>
        </main>
    </div>
</body>

</html>