<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Detail</title>
</head>

<body>
    <div class="layout">
        <%- include("../partials/_navbar") %>
        <main class="content">
            <section class="event-index">
                <h1 class="heading">Payment Details for <%= event.name %></h1>
                <p><strong>Amount:</strong> $<%= payment.amount %>
                </p>
                <p><strong>Type:</strong>
                    <%= payment.paymentType %>
                </p>
                <p><strong>Installment:</strong>
                    <%= payment.instalmentNumber || 'N/A' %> / <%= payment.totalInstalment || 'N/A' %>
                </p>
                <p><strong>Due:</strong>
                    <%= payment.dueDate?.toLocaleDateString() || 'N/A' %>
                </p>
                <p><strong>Paid:</strong>
                    <%= payment.paidDate?.toLocaleDateString() || 'N/A' %>
                </p>
                <p><strong>Status:</strong>
                    <%= payment.status %>
                </p>
                <p><strong>Notes:</strong>
                    <%= payment.notes %>
                </p>
                <a class="btn btn-edit"  href="/events/<%= eventId %>/vendors/<%= userVendorId %>/payments/<%= payment._id %>/edit">Edit</a>
                <a class="btn btn-back" href="/events/<%= eventId %>/payments">Back to Payments</a>

            </section>
        </main>
    </div>
</body>

</html>