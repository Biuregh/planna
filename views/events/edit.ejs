<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= event._id %></title>
</head>

<body>
    <section class="event-edit">
        <h1 class="heading">Edit Event</h1>
        <form class="event-form" action="/events/<%= event._id %>?_method=PUT" method="POST">
            <label class="form-label">Name:</label>
            <input class="form-input" name="name" value="<%= event.name %>" required />

            <label class="form-label">Date:</label>
            <input class="form-input" type="date" name="date" value="<%= event.date.toISOString().substring(0,10) %>"
                required />

            <label class="form-label">Location:</label>
            <input class="form-input" name="location" value="<%= event.location %>" />

            <label class="form-label">Notes:</label>
            <textarea class="form-textarea" name="notes"><%= event.notes %></textarea>

            <button class="btn btn-submit" type="submit">Update Event</button>
        </form>
        <a class="btn btn-cancel" href="/events/<%= event._id %>">← Cancel</a>
    </section>

</body>

</html>